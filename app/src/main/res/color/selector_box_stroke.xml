<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state (darker cyan) -->
    <item android:state_focused="true" android:color="#0088AA" />
    <!-- Default unfocused state (brand_cyan) -->
    <item android:color="@color/brand_cyan" />
</selector>
